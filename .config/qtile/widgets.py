from libqtile import widget


widget_defaults = dict(
    font="Terminess Nerd Font Regular",
    fontsize=14,
    padding=3,
)

grp_box = widget.GroupBox(highlight_method='block',
		    	    	  disable_drag=True,
                          font="Terminess Nerd Font Regular")
